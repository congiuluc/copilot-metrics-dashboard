import { PageHeader } from "@/features/page-header/page-header";

export const Header = () => {
  return (
    <PageHeader
      title="Premium Requests"
      description="Visualize and analyze GitHub Copilot premium request data from uploaded CSV files."
    />
  );
};